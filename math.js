const add = (a, b) => {
  return a + b;
};

const sub = (a, b) => {
  return a - b;
};

module.exports = {
  add: add, // it can also be written as add
  sub: sub, // it can also be written as sub
};
