import { upperCase } from "upper-case";

function greet(name) {
  console.log(upperCase(`Hello ${name}, welcome to Codevolution`));
}

export { greet };

// npm adduser <username> // sign with proper details

// npm publish // to publish the package
